# DataFlow360 — dépendances d'exécution
# GROUPE 2 · Sprint 0 · responsable : Ndeye Penda SARR
#
# Règle d'équipe : une dépendance entre dans ce fichier par la demande de fusion
# de celui qui en a besoin, au sprint où il en a besoin. On n'installe pas
# aujourd'hui ce qui ne servira que dans trois sprints.
#
# Python 3.11. Les bornes ci-dessous sont volontairement larges ; les versions
# exactes sont figées dans requirements.lock après la vérification croisée.

# --- Socle commun : lecture, configuration ----------------------------------
pandas>=2.1,<3
python-dotenv>=1.0
pyyaml>=6.0

# --- Accès aux trois bases ---------------------------------------------------
sqlalchemy>=2.0,<3
psycopg2-binary>=2.9,<3
pymongo>=4.6,<5
elasticsearch>=8.12,<9        # le client doit rester en version 8, comme le serveur

# --- Flux d'événements -------------------------------------------------------
confluent-kafka>=2.3,<3
faker>=24.0

# --- À ajouter par leur responsable, au sprint indiqué -----------------------
# great-expectations         SW · Sprint 2 · version à choisir selon pandas
# langdetect                 AD · Sprint 2 · détection de langue du catalogue
# scikit-learn               BD · Sprint 5 · modèle de ré-achat
# imbalanced-learn           BD · Sprint 5 · rééquilibrage des classes
#
# NE PAS ajouter apache-airflow ici : Airflow tourne dans sa propre image,
# et ses contraintes de versions entreraient en conflit avec ce fichier.
